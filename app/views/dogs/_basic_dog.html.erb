<div class="col-md-3 well text-center">
  <% if dog.avatar.url != nil %>
    <div class="row"><%= image_tag dog.avatar.url, size:200 %></div>
  <% end %>
  <div class="row"><h3><%= dog.name %></h3></div>
</div>
